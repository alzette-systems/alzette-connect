{
  "name": "@alzette/connect-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode production",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@wailsio/runtime": "3.0.0-beta.9"
  },
  "devDependencies": {
    "vite": "8.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
